# Screenshots

After the first CloudFront deployment, capture the production UI here. Suggested filenames:

- `pixeldrop-home.png` — initial upload screen
- `pixeldrop-processing.png` — active resize state
- `pixeldrop-complete.png` — completed download state

The root README links to this directory so a repository visitor can inspect visual evidence of the deployed application.
